{
  "name": "rust-kanban",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @rust-kanban/web dev",
    "dev:desktop": "pnpm --filter @rust-kanban/desktop dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
